---
- name: Provision Cloud Environment
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Display provisioning request
      ansible.builtin.debug:
        msg:
          - "Environment: {{ environment_name }}"
          - "Instance Size: {{ instance_size }}"
          - "Region: {{ region }}"
          - "TTL: {{ ttl_hours }} hours"
          - "Database Engine: {{ database_engine }}"
          - "Monitoring: {{ enable_monitoring }}"

    - name: Sending request for approval
      ansible.builtin.uri:
        url: "{{ lookup('env', 'CONTROLLER_HOST') }}/api/controller/v2/workflow_job_templates/45/launch/"
        method: POST
        user: "{{ lookup('env', 'CONTROLLER_USERNAME') }}"
        password: "{{ lookup('env', 'CONTROLLER_PASSWORD') }}"
        force_basic_auth: true
        validate_certs: false
        body_format: json
        body: {}
        status_code: 201
      register: launch_result
      no_log: true

    - name: Approval pending
      ansible.builtin.uri:
        url: "{{ lookup('env', 'CONTROLLER_HOST') }}/api/controller/v2/workflow_jobs/{{ launch_result.json.id }}/"
        method: GET
        user: "{{ lookup('env', 'CONTROLLER_USERNAME') }}"
        password: "{{ lookup('env', 'CONTROLLER_PASSWORD') }}"
        force_basic_auth: true
        validate_certs: false
      register: wf_status
      until: wf_status.json.status in ['successful', 'failed', 'error', 'canceled']
      retries: 360
      delay: 10
      no_log: true

    - name: Approval denied
      ansible.builtin.fail:
        msg: "Request was denied or timed out"
      when: wf_status.json.status != 'successful'

    - name: Approval granted
      ansible.builtin.debug:
        msg: "Approved. Provisioning {{ environment_name }} in {{ region }}."

    - name: Configure instance ({{ instance_size }})
      ansible.builtin.debug:
        msg: "Launching {{ instance_size }} instance in {{ region }}"

    - name: Configure database
      ansible.builtin.debug:
        msg: "Setting up {{ database_engine }} database"
      when: database_engine != "none"

    - name: Configure monitoring
      ansible.builtin.debug:
        msg: "Enabling monitoring for {{ environment_name }}"
      when: enable_monitoring == "yes"

    - name: Set TTL auto-shutdown
      ansible.builtin.debug:
        msg: "Environment {{ environment_name }} will auto-terminate in {{ ttl_hours }} hours"

    - name: Provisioning complete
      ansible.builtin.debug:
        msg: "Environment {{ environment_name }} is ready in {{ region }}"
